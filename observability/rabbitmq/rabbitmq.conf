# Prometheus documentation recommends a 10-second collection interval paired
# with a 15-second Prometheus scrape. The dedicated endpoint stays internal to
# the Compose network; Prometheus is its only consumer.
collect_statistics_interval = 10000
prometheus.tcp.port = 15692
